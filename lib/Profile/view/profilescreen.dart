import 'package:flutter/material.dart';


class Profil extends StatelessWidget {
  static final pageName='/Profil';
  const Profil({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
